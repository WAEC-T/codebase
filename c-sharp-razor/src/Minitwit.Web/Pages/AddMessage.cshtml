@page "/add_message"
@model Minitwit.Web.Pages.MyTimelineModel

@{  
    Layout = null;
    var currentUser = HttpContext.Session.GetString("UserName") ?? User.Identity?.Name;
    await Model.OnPostCreateMessage();
    Response.Redirect($"/{currentUser}");
}
