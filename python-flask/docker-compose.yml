services:
  python-flask:
    image: waect/python-flask
    build: .
    container_name: python-flask
    environment:
      - AWS_DATABASE_URL
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    networks:
      - waect-network

networks:
  waect-network:
    name: waect-network
    driver: bridge
    external: true