
name: Build and Push Docker Image WEAC Codebase

on:
  push:
    branches:
      - feat/cd-build-push
    paths:
      - 'python-flask/**'
      - 'c-sharp-razor/**'
      - 'rust-actix/**'
      - 'ruby-sinatra/**'
      - 'javascript-express/**'
      - 'go-gin/**'
      - 'go-gorilla/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - python-flask
          - c-sharp-razor
          - rust-actix
          - ruby-sinatra
          - javascript-express
          - go-gin
          - go-gorilla

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: '0'

      - name: Determine if Service Needs Build Compared to Main
        id: check_changes
        run: |
          git fetch origin main

          if git diff --name-only origin/main...HEAD | grep -q "^${{ matrix.service }}/"; then
            echo "Changes detected in ${matrix.service} compared to main."
            echo "service_changed=true" >> $GITHUB_ENV
          else
            echo "No changes detected in ${matrix.service} compared to main."
            echo "service_changed=false" >> $GITHUB_ENV
          fi

      - name: Set up Docker Buildx
        if: env.service_changed == 'true'
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        if: env.service_changed == 'true'
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Check if Dockerfile exists
        if: env.service_changed == 'true'
        id: check_file
        run: |
          if [ -d "./${{ matrix.service }}" ] && [ -f "./${{ matrix.service }}/docker-compose.yml" ]; then
            echo "found=true" >> $GITHUB_ENV
          else
            echo "found=false" >> $GITHUB_ENV
          fi

      - name: Install Docker Compose
        if: env.found == 'true'
        run: |
          curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          chmod +x /usr/local/bin/docker-compose

      - name: Build and Push Docker Image
        if: env.found == 'true'
        run: |
          cd ${{ matrix.service }}
          docker-compose build
          IMAGE_NAME="waect/${{ matrix.service }}"
          echo "Built image name: ${IMAGE_NAME}"
          
          docker images
          
          COMMIT_SHA=$(git rev-parse --short HEAD)
          TIMESTAMP=$(date +%Y%m%d%H%M)
              
          docker tag ${IMAGE_NAME}:latest simonharwick97822/${{ matrix.service }}:latest
          docker tag ${IMAGE_NAME}:latest simonharwick97822/${{ matrix.service }}:commit-sha-${COMMIT_SHA}-${TIMESTAMP}

      - name: Push Docker Image
        if: env.found == 'true'
        run: |
          COMMIT_SHA=$(git rev-parse --short HEAD)
          TIMESTAMP=$(date +%Y%m%d%H%M)
          docker push simonharwick97822/${{ matrix.service }}:latest
          docker push simonharwick97822/${{ matrix.service }}:commit-sha-${COMMIT_SHA}-${TIMESTAMP}
